# PostgreSQL Patterns

- **Top customers (last 365 days)** — see `sql/postgres/queries/top_customers.sql`.
- **Useful indexes** — B-Tree on join/where columns, partial indexes on boolean flags, multicolumn index order by selectivity.
